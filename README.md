To run original TensoRF, install all requirements and `icecream`

```
python run.py --config ./configs/lego.txt
```

Note that you might need to change config file.

This repo could potentially now be not bug-free.

